#pragma once
/* Generated by netkit AOT compiler (quant lowered) — static CmsisQuantPlan call chain */

#include "arena.hpp"

#include <cstddef>
#include <cstdint>

namespace netkit::aot::mnist_cnn_int8 {

inline constexpr const char* kName = "mnist_cnn_int8";
inline constexpr const char* kNetwork = "cnn";
inline constexpr bool kLowered = true;
inline constexpr bool kQuantLowered = true;
inline constexpr std::uint32_t kInputElements = 784u;
inline constexpr std::uint32_t kOutputElements = 10u;
inline constexpr std::uint32_t kInputShape[] = {28, 28, 1};
inline constexpr std::uint32_t kInputRank = 3u;
inline constexpr float kInputScale = 0.00392157f;
inline constexpr int32_t kInputZeroPoint = -128;
inline constexpr std::size_t kActAElements = 21632u;
inline constexpr std::size_t kActBElements = 5408u;
inline constexpr std::size_t kWorkspaceBytes = 1152u;
inline constexpr std::size_t kArenaBytesAfterLoad = 0u;
inline constexpr std::size_t kArenaBytesAfterForward = 0u;
inline constexpr std::size_t kArenaBytesRecommended = 64u;
inline constexpr std::size_t kNkBytes = 0u;

inline bool InitArena(Arena& arena, void* memory, std::size_t capacity)
{
    if (!memory || capacity < kArenaBytesRecommended)
        return false;
    arena.init(memory, capacity);
    return true;
}

class Model {
public:
    Model() = default;

    bool load(Arena& arena);
    bool forward(Arena& arena, const float* input, int8_t* output) const;
    bool forwardInt8(Arena& arena, const int8_t* input, int8_t* output) const;
    [[nodiscard]] bool isLoaded() const { return loaded_; }

private:
    bool loaded_ = false;
};

}  // namespace netkit::aot::mnist_cnn_int8
